import { FadeInOnScroll } from './fade-in-on-scroll';

describe('FadeInOnScroll', () => {
  it('should create an instance', () => {
    const directive = new FadeInOnScroll();
    expect(directive).toBeTruthy();
  });
});
